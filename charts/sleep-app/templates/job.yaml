apiVersion: batch/v1
kind: Job
metadata:
  name: {{ randAlpha 8 | lower }}
spec:
  template:
    spec:
      containers:
        - name: upgrade-sql-schema
          image: alpine:latest
          command: ["sleep", "15"]
      restartPolicy: Never
